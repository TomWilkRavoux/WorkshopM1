sonar.organization=workshopepsi
sonar.projectKey=TomWilkRavoux_WorkshopM1
sonar.projectName=Workshop

# =====================================================
# 📁 SOURCES ET EXCLUSIONS
# =====================================================

# Analyse du code dans le frontend (React/Vite) et backend (Flask)
sonar.sources=frontend,backend

# Exclusions pour éviter les fichiers générés ou non pertinents
sonar.exclusions=**/node_modules/**,**/dist/**,**/build/**,**/.venv/**,**/venv/**,**/__pycache__/**,**/*.spec.ts,**/*.test.ts,**/*.test.js,**/*.test.jsx

# =====================================================
# 🧪 TESTS ET COUVERTURE DE CODE
# =====================================================

# --- FRONTEND (React/Vite + Vitest) ---
# Rapport LCOV généré par Vitest (npm run test:ci)
sonar.javascript.lcov.reportPaths=frontend/coverage/lcov.info

# --- BACKEND (Flask + pytest + coverage) ---
# Rapport XML généré par pytest-cov
sonar.python.coverage.reportPaths=backend/coverage.xml

# =====================================================
# 🌐 LANGAGES ET OPTIONS
# =====================================================

# TypeScript config (si tu en as une)
sonar.typescript.tsconfigPaths=frontend/tsconfig.json

# Version de Python utilisée
sonar.python.version=3.11

# =====================================================
# 🧹 OPTIONNEL : FICHIERS IGNORÉS PAR DÉFAUT
# =====================================================
# Tu peux ignorer les fichiers de config, scripts de déploiement, etc.
# sonar.test.exclusions=**/*.test.*,**/__tests__/**,**/tests/**
